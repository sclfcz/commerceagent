## 改了什么

<!-- 用一两句话说明本 PR 的改动范围；一个 PR 只解决一个问题 -->

## 为什么

<!-- 背景、动机、关联的 Issue。关联请用 Closes #<编号> -->

## 怎么验证的

<!-- 贴出实际执行的命令与结果；行为变化请说明新增/修改了哪些测试 -->

- [ ] `make typecheck`
- [ ] `make test`
- [ ] `make format-check`
- [ ] 改动 `desktop/` 时已本地打包验证（`npm --prefix desktop run dist:dir`）

## 影响范围

<!-- 是否影响兼容性、配置、数据迁移、桌面端/容器模式等；没有请写“无” -->

## 检查清单

- [ ] 改动聚焦，没有混入无关格式化/重构/依赖升级
- [ ] 未提交密钥、运行时数据（`data/`、日志）或构建产物
- [ ] 已补充或更新测试（如适用）
- [ ] 已更新相关文档（如适用）
